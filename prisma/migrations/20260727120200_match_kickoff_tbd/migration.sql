-- «Время уточняется»: генератор расписания расставляет только даты туров,
-- а точное время организаторы согласуют позже.
--
-- Значение по умолчанию false делает все существующие матчи корректными:
-- у них время назначено, поэтому переносить данные не нужно.
ALTER TABLE "Match" ADD COLUMN "kickoffTbd" BOOLEAN NOT NULL DEFAULT false;
