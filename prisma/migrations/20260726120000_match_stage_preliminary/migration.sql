-- Предварительный раунд плей-офф: стыки за право пройти дальше.
--
-- Вынесено в отдельную миграцию намеренно: PostgreSQL не разрешает
-- использовать только что добавленное значение enum в той же транзакции,
-- где оно объявлено. Следующая миграция уже может на него ссылаться.
ALTER TYPE "MatchStage" ADD VALUE 'PRELIMINARY' AFTER 'REGULAR';
